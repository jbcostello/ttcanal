from setuptools import setup

setup(name='ttcanal',
      #version='1.0',
      #url = 'https://github.com/jbcostello/ttcanal',
      #author = 'Joe Costello',
      #author_email = 'jbcostello@physics.ucsb.edu',
      packages = [],
      install_requires = [],
      #entry_points={'console_scripts': ['runPEPS = PEPS.command_line:main']},
      include_package_data = True,
      zip_safe = False)